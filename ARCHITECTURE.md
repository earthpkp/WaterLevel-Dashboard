# System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│                     http://localhost:3000                        │
└─────────────────────┬───────────────────────────────────────────┘
                      │
                      │ HTTP Requests
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                     FRONTEND CONTAINER                           │
│                      (React App)                                 │
│  ┌────────────────────────────────────────────────────────┐     │
│  │ Components:                                             │     │
│  │  - App.js (Main)                                        │     │
│  │  - StationCard.js (Station Display)                    │     │
│  │  - WaterLevelChart.js (Chart)                          │     │
│  │  - AlertsPanel.js (Alerts)                             │     │
│  │  - Statistics.js (Stats)                               │     │
│  └────────────────────────────────────────────────────────┘     │
│                                                                   │
│  Port: 3000                                                      │
│  Volume: ./frontend:/app                                         │
└─────────────────────┬───────────────────────────────────────────┘
                      │
                      │ API Calls (Axios)
                      │ http://localhost:5000/api/*
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                     BACKEND CONTAINER                            │
│                  (Node.js + Express API)                         │
│  ┌────────────────────────────────────────────────────────┐     │
│  │ API Endpoints:                                          │     │
│  │  GET  /api/health                                       │     │
│  │  GET  /api/metadata                                     │     │
│  │  GET  /api/waterlevel                                   │     │
│  │  GET  /api/waterlevel/latest                            │     │
│  │  GET  /api/waterlevel/chart                             │     │
│  │  GET  /api/statistics/:station                          │     │
│  │  GET  /api/alerts                                       │     │
│  └────────────────────────────────────────────────────────┘     │
│                                                                   │
│  Port: 5000                                                      │
│  Volume: ./backend:/app                                          │
└─────────────────────┬───────────────────────────────────────────┘
                      │
                      │ SQL Queries (MySQL2)
                      │ mysql://wateruser:***@mysql:3306
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                     DATABASE CONTAINER                           │
│                       (MySQL 8.0)                                │
│  ┌────────────────────────────────────────────────────────┐     │
│  │ Database: waterlevel_db                                 │     │
│  │                                                          │     │
│  │ Tables:                                                  │     │
│  │  ┌─────────────────────────────────────────────┐       │     │
│  │  │ metadata_kolok_waterlevel                    │       │     │
│  │  │  - id (PK)                                   │       │     │
│  │  │  - station_id                                │       │     │
│  │  │  - lat, lon                                  │       │     │
│  │  │  - bank_level                                │       │     │
│  │  │  - subbasin_name                             │       │     │
│  │  └─────────────────────────────────────────────┘       │     │
│  │                                                          │     │
│  │  ┌─────────────────────────────────────────────┐       │     │
│  │  │ waterlevel_data                              │       │     │
│  │  │  - id (PK)                                   │       │     │
│  │  │  - date_time                                 │       │     │
│  │  │  - x_274                                     │       │     │
│  │  │  - x_119a                                    │       │     │
│  │  │  - x_119                                     │       │     │
│  │  └─────────────────────────────────────────────┘       │     │
│  └────────────────────────────────────────────────────────┘     │
│                                                                   │
│  Port: 3306                                                      │
│  Volume: mysql_data:/var/lib/mysql                              │
│  Init: ./init-mysql.sql                                          │
└───────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                         NETWORK                                  │
│                   waterdashboard-network                         │
│                      (Bridge Driver)                             │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow

```
1. User opens http://localhost:3000
        ↓
2. React App loads
        ↓
3. App.js calls API endpoints via Axios
        ↓
4. Backend (Express) receives HTTP requests
        ↓
5. Backend queries MySQL database
        ↓
6. MySQL returns data
        ↓
7. Backend formats and sends JSON response
        ↓
8. React components update with data
        ↓
9. User sees beautiful dashboard with charts
```

## Technology Stack Overview

```
┌───────────────────────────────────────────────────────────┐
│                      FRONTEND                              │
│  React 18                                                  │
│  ├── Recharts (Charts)                                    │
│  ├── Axios (HTTP Client)                                  │
│  └── CSS3 (Styling)                                       │
└───────────────────────────────────────────────────────────┘
                         │
                         │ REST API
                         ▼
┌───────────────────────────────────────────────────────────┐
│                      BACKEND                               │
│  Node.js v18                                              │
│  ├── Express.js (Web Framework)                           │
│  ├── MySQL2 (Database Driver)                             │
│  ├── CORS (Cross-Origin Resource Sharing)                 │
│  └── dotenv (Environment Variables)                       │
└───────────────────────────────────────────────────────────┘
                         │
                         │ SQL
                         ▼
┌───────────────────────────────────────────────────────────┐
│                      DATABASE                              │
│  MySQL 8.0                                                │
│  ├── InnoDB Engine                                        │
│  ├── UTF8MB4 Charset                                      │
│  └── Indexes on date_time & PKs                           │
└───────────────────────────────────────────────────────────┘
```

## Port Mapping

```
Host                Container           Service
────────────────────────────────────────────────────────────
localhost:3000  →   3000                Frontend (React)
localhost:5000  →   5000                Backend (Express API)
localhost:3306  →   3306                Database (MySQL)
```

## File Structure Map

```
waterdashboard/
│
├── 🐳 Docker Files
│   ├── docker-compose.yml      ← Main orchestration
│   ├── .env                    ← Environment config
│   └── .gitignore
│
├── 🗄️ Database Files
│   ├── init-mysql.sql          ← MySQL init script
│   ├── kolok_waterlevel.sql    ← Original data
│   ├── metadata_kolok_waterlevel.csv
│   └── waterlevel_data.csv
│
├── 🔧 Backend
│   ├── server.js               ← Express API
│   ├── package.json
│   ├── Dockerfile
│   └── .dockerignore
│
├── 💻 Frontend
│   ├── public/
│   │   └── index.html
│   ├── src/
│   │   ├── App.js              ← Main app
│   │   ├── index.js
│   │   ├── index.css
│   │   └── components/
│   │       ├── StationCard.js
│   │       ├── WaterLevelChart.js
│   │       ├── AlertsPanel.js
│   │       └── Statistics.js
│   ├── package.json
│   ├── Dockerfile
│   └── .dockerignore
│
├── 📚 Documentation
│   ├── README.md               ← Main docs
│   ├── QUICKSTART.md           ← Quick reference
│   ├── PROJECT_SUMMARY.md      ← This summary
│   └── ARCHITECTURE.md         ← This file
│
├── 🛠️ Scripts
│   ├── setup.ps1               ← Setup automation
│   ├── test-api.ps1            ← API testing
│   └── convert_sql.py          ← SQL converter
│
└── ⚙️ VS Code Config
    └── .vscode/
        ├── settings.json
        └── extensions.json
```

## Component Interaction

```
┌──────────────────────────────────────────────────────────┐
│                        App.js                             │
│                     (Main Container)                      │
│                                                            │
│  ┌──────────────────────────────────────────────────┐   │
│  │ useEffect: Fetch data on mount                    │   │
│  │  - Metadata                                        │   │
│  │  - Latest water levels                            │   │
│  │  - Alerts                                          │   │
│  │  - Chart data                                      │   │
│  └──────────────────────────────────────────────────┘   │
│                                                            │
│  ┌──────────────────────────────────────────────────┐   │
│  │ State Management                                  │   │
│  │  - metadata: []                                    │   │
│  │  - latestData: {}                                  │   │
│  │  - chartData: []                                   │   │
│  │  - alerts: []                                      │   │
│  │  - loading: boolean                                │   │
│  │  - error: string                                   │   │
│  │  - dateRange: {start, end}                         │   │
│  └──────────────────────────────────────────────────┘   │
│                                                            │
│  ┌──────────────────────────────────────────────────┐   │
│  │ Child Components                                  │   │
│  │                                                    │   │
│  │  AlertsPanel                                      │   │
│  │    ↓ props: alerts                                │   │
│  │                                                    │   │
│  │  StationCard (× 3)                                │   │
│  │    ↓ props: station, currentLevel, status         │   │
│  │                                                    │   │
│  │  Statistics                                       │   │
│  │    ↓ props: metadata                              │   │
│  │                                                    │   │
│  │  WaterLevelChart                                  │   │
│  │    ↓ props: data, dateRange, setDateRange, meta  │   │
│  └──────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────┘
```

## API Response Format

```javascript
// GET /api/metadata
{
  "success": true,
  "data": [
    {
      "id": 1,
      "station_id": "X.274",
      "lat": 5.839205,
      "lon": 101.892307,
      "bank_level": 23.5,
      "subbasin_name": "kolok"
    },
    // ... more stations
  ]
}

// GET /api/waterlevel/latest
{
  "success": true,
  "data": {
    "id": 2831,
    "date_time": "2025-10-25T00:00:00.000Z",
    "x_274": 19.85,
    "x_119a": 7.76,
    "x_119": 4.89
  }
}

// GET /api/alerts
{
  "success": true,
  "data": [
    {
      "station_id": "X.119",
      "station_name": "kolok",
      "water_level": 6.5,
      "bank_level": 7.0,
      "percentage": "92.86",
      "severity": "warning",
      "date_time": "2025-10-25T00:00:00.000Z"
    }
  ],
  "count": 1
}
```

## Deployment Options

```
┌────────────────────────────────────────────────────────┐
│                    DEVELOPMENT                          │
│                 (Current Setup)                         │
│                                                          │
│  docker-compose up -d                                   │
│  ├── Frontend: localhost:3000                           │
│  ├── Backend: localhost:5000                            │
│  └── Database: localhost:3306                           │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│                    PRODUCTION                           │
│                  (Future Options)                       │
│                                                          │
│  Option 1: Cloud VM (AWS EC2, Azure VM, GCP CE)        │
│  ├── Install Docker                                     │
│  ├── Setup domain & SSL                                 │
│  └── Configure firewall                                 │
│                                                          │
│  Option 2: Container Services                           │
│  ├── AWS ECS/Fargate                                    │
│  ├── Azure Container Instances                          │
│  └── Google Cloud Run                                   │
│                                                          │
│  Option 3: Kubernetes                                   │
│  ├── AWS EKS                                            │
│  ├── Azure AKS                                          │
│  └── Google GKE                                         │
└────────────────────────────────────────────────────────┘
```

---

**Architecture designed by GitHub Copilot 🤖**
