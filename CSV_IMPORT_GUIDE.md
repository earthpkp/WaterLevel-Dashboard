# CSV Import Feature - คู่มือการใช้งาน

## 📤 การนำเข้าข้อมูล Forecast จากไฟล์ CSV

### ฟีเจอร์ใหม่
ตอนนี้คุณสามารถนำเข้าข้อมูล Forecast จากไฟล์ CSV ได้แล้ว!

### รูปแบบไฟล์ CSV

#### ตัวอย่างไฟล์ที่ถูกต้อง

```csv
date_time,x_274,x_119a,x_119
2025-11-12,18.45,4.95,3.15
2025-11-13,18.50,5.00,3.20
2025-11-14,18.55,5.05,3.25
2025-11-15,18.60,5.10,3.30
```

#### รูปแบบที่รองรับ

**รูปแบบคอลัมน์ที่รองรับ:**
- `date_time` หรือ `date` - วันที่ของข้อมูล (บังคับ)
- `x_274` หรือ `x.274` - ระดับน้ำสถานี X.274
- `x_119a` หรือ `x.119a` - ระดับน้ำสถานี X.119A
- `x_119` หรือ `x.119` - ระดับน้ำสถานี X.119

**รูปแบบวันที่ที่รองรับ:**
- `YYYY-MM-DD` (แนะนำ) - เช่น `2025-11-12`
- `YYYY/MM/DD` - เช่น `2025/11/12`
- `DD/MM/YYYY` - เช่น `12/11/2025`

**หมายเหตุ:**
- ค่าระดับน้ำเป็นตัวเลขทศนิยม (เช่น 18.45)
- สามารถมีค่าว่าง (null) ได้
- ระบบจะข้ามแถวที่มีข้อมูลไม่ครบถ้วนหรือรูปแบบผิด

### วิธีการใช้งาน

#### 1. เตรียมไฟล์ CSV

สร้างไฟล์ CSV ตามรูปแบบด้านบน คุณสามารถใช้:
- Microsoft Excel → Save As → CSV (Comma delimited)
- Google Sheets → File → Download → CSV
- Text Editor (Notepad, VS Code, etc.)

#### 2. นำเข้าข้อมูลผ่านหน้าเว็บ

1. เปิดเว็บไซต์ → http://localhost:3000
2. คลิกเมนู "📊 ข้อมูล Observe"
3. คลิกปุ่ม "📤 นำเข้า Forecast CSV"
4. เลือกไฟล์ CSV ที่เตรียมไว้
5. ยืนยันการนำเข้า
6. รอข้อความแจ้งผลลัพธ์

#### 3. ตรวจสอบผลลัพธ์

ระบบจะแสดงผลลัพธ์:
- ✅ **ข้อมูลใหม่**: จำนวนรายการที่เพิ่มใหม่
- ✅ **อัพเดต**: จำนวนรายการที่อัพเดตข้อมูลเดิม
- ⚠️ **ข้าม**: จำนวนรายการที่ระบบข้ามไป (เนื่องจากข้อมูลผิดพลาด)

### การนำเข้าผ่าน API

#### ใช้ cURL (Command Line)

```bash
curl -X POST http://localhost:5000/api/import/forecast/csv \
  -H "Content-Type: multipart/form-data" \
  -F "file=@forecast_data.csv"
```

#### ใช้ PowerShell

```powershell
$file = "C:\path\to\forecast_data.csv"
Invoke-WebRequest -Method POST -Uri "http://localhost:5000/api/import/forecast/csv" `
  -InFile $file `
  -ContentType "multipart/form-data"
```

#### ใช้ JavaScript/Axios

```javascript
const formData = new FormData();
formData.append('file', fileInput.files[0]);

axios.post('http://localhost:5000/api/import/forecast/csv', formData, {
  headers: { 'Content-Type': 'multipart/form-data' }
})
.then(response => {
  console.log('Import success:', response.data);
})
.catch(error => {
  console.error('Import error:', error);
});
```

### Response Format

**สำเร็จ (200 OK):**
```json
{
  "success": true,
  "message": "Forecast data imported successfully from CSV",
  "imported": 5,
  "updated": 2,
  "skipped": 0,
  "total": 7
}
```

**ผิดพลาด (400/500):**
```json
{
  "success": false,
  "error": "Failed to import CSV file",
  "details": "CSV must have a 'date_time' or 'date' column"
}
```

### ข้อจำกัดและข้อควรระวัง

#### ข้อจำกัด
- **ขนาดไฟล์**: สูงสุด 10 MB
- **ประเภทไฟล์**: เฉพาะ `.csv` เท่านั้น
- **Encoding**: UTF-8 (แนะนำ)
- **Separator**: Comma (,) เท่านั้น

#### ข้อควรระวัง
- ✅ **ตรวจสอบรูปแบบวันที่ให้ถูกต้อง**
- ✅ **ใช้ตัวเลขทศนิยมสำหรับค่าระดับน้ำ**
- ✅ **ไม่ใส่เครื่องหมาย comma ในค่าข้อมูล**
- ✅ **ตรวจสอบชื่อคอลัมน์ให้ตรงตามที่กำหนด**
- ⚠️ **ข้อมูลซ้ำกันจะถูกอัพเดตทับ (ตาม date_time)**

### ตัวอย่างไฟล์ CSV

ไฟล์ตัวอย่างพร้อมใช้งาน:
- 📁 `data/forecast_sample.csv` - ตัวอย่างข้อมูล forecast 7 วัน

### Error Handling

#### ข้อผิดพลาดที่พบบ่อย

**1. "No file uploaded"**
- สาเหตุ: ไม่ได้เลือกไฟล์
- แก้ไข: เลือกไฟล์ CSV ก่อนคลิกอัปโหลด

**2. "Only CSV files are allowed"**
- สาเหตุ: ไฟล์ไม่ใช่ .csv
- แก้ไข: แปลงไฟล์เป็น CSV ก่อน

**3. "CSV file is empty or has no data rows"**
- สาเหตุ: ไฟล์ว่างหรือมีแค่ header
- แก้ไข: เพิ่มข้อมูลอย่างน้อย 1 แถว

**4. "CSV must have a 'date_time' or 'date' column"**
- สาเหตุ: ไม่มีคอลัมน์วันที่
- แก้ไข: เพิ่มคอลัมน์ `date_time` หรือ `date`

**5. "Invalid date format"**
- สาเหตุ: รูปแบบวันที่ไม่ถูกต้อง
- แก้ไข: ใช้รูปแบบ YYYY-MM-DD, YYYY/MM/DD หรือ DD/MM/YYYY

### การทดสอบ

#### ทดสอบด้วยไฟล์ตัวอย่าง

1. ใช้ไฟล์ `data/forecast_sample.csv`
2. อัปโหลดผ่านหน้าเว็บ
3. ตรวจสอบว่ามีข้อมูล 7 รายการ
4. ตรวจสอบในฐานข้อมูล:

```sql
SELECT date_time, x_274, x_119a, x_119, data_type 
FROM waterlevel_data 
WHERE data_type = 'forecast' 
ORDER BY date_time DESC 
LIMIT 10;
```

#### ทดสอบผ่าน Docker

```bash
# เข้าไปใน container
docker exec -it waterdashboard-mysql mysql -uroot -prootpassword waterlevel_db

# Query ข้อมูล forecast
SELECT COUNT(*) FROM waterlevel_data WHERE data_type = 'forecast';
```

### Best Practices

#### 1. การเตรียมข้อมูล
- ตรวจสอบข้อมูลก่อนนำเข้า
- ลบแถวที่ซ้ำกันออก
- ตรวจสอบค่าผิดปกติ (outliers)

#### 2. การนำเข้าข้อมูล
- นำเข้าทีละน้อย (แนะนำไม่เกิน 1000 แถวต่อครั้ง)
- สำรองข้อมูลเดิมก่อนนำเข้าข้อมูลใหม่
- ทดสอบกับไฟล์ขนาดเล็กก่อน

#### 3. การตรวจสอบ
- ตรวจสอบผลลัพธ์หลังนำเข้า
- ดูกราฟเพื่อยืนยันว่าข้อมูลถูกต้อง
- ตรวจสอบจำนวนรายการที่ถูกข้าม

### Troubleshooting

#### ปัญหา: ข้อมูลไม่แสดงในกราฟ

**วิธีแก้:**
1. ตรวจสอบช่วงวันที่ในหน้าเว็บ
2. กดปุ่ม "🔄 รีเฟรชข้อมูล"
3. ตรวจสอบในฐานข้อมูลว่ามีข้อมูลจริง

#### ปัญหา: ไฟล์อัปโหลดไม่ได้

**วิธีแก้:**
1. ตรวจสอบขนาดไฟล์ (ต้องไม่เกิน 10 MB)
2. ตรวจสอบนามสกุลไฟล์ (.csv)
3. ตรวจสอบว่า backend ทำงานอยู่
4. ดู logs: `docker logs waterdashboard-backend`

#### ปัญหา: ข้อมูลบางแถวถูกข้าม

**วิธีแก้:**
1. ตรวจสอบ backend logs เพื่อดูสาเหตุ
2. แก้ไขรูปแบบวันที่ในแถวที่มีปัญหา
3. ตรวจสอบว่าข้อมูลครบถ้วน

### Support

หากพบปัญหาหรือต้องการความช่วยเหลือ:
1. ตรวจสอบ logs: `docker logs waterdashboard-backend`
2. ตรวจสอบ API_STATUS.md สำหรับข้อมูลล่าสุด
3. ทดสอบ API endpoint: `http://localhost:5000/api/import/forecast/csv`

---

**Last Updated**: November 11, 2025  
**Feature Status**: ✅ Production Ready
