# คู่มือการดึงข้อมูลจาก API แบบช่วงวันที่

## ภาพรวม

ระบบรองรับการดึงข้อมูล Observe จาก API ของ กรมชลประทาน ทั้งแบบวันเดียวและแบบช่วงวันที่ (Date Range)

## วิธีใช้งานผ่าน Frontend

### 1. การดึงข้อมูลวันเดียว

1. คลิกปุ่ม "📡 ดึงข้อมูลจาก API"
2. จะแสดง dialog สำหรับเลือกวันที่
3. เลือกวันที่เริ่มต้นจาก date picker (ระบบจะแสดงปฏิทิน)
4. เว้นช่องวันที่สิ้นสุดไว้ว่าง
5. คลิก "✅ ยืนยันนำเข้า"

### 2. การดึงข้อมูลช่วงวันที่

1. คลิกปุ่ม "📡 ดึงข้อมูลจาก API"
2. จะแสดง dialog สำหรับเลือกวันที่
3. เลือกวันที่เริ่มต้นจาก date picker
4. เลือกวันที่สิ้นสุดจาก date picker
5. คลิก "✅ ยืนยันนำเข้า"

**หมายเหตุ:** 
- ระบบจะแปลงวันที่จาก ค.ศ. เป็น พ.ศ. อัตโนมัติ
- Date picker ใช้งานง่าย ไม่ต้องพิมพ์วันที่เอง
- ระบบจะดึงข้อมูลทีละวัน และรอ 1 วินาทีระหว่างการเรียก API แต่ละครั้ง เพื่อป้องกัน rate limit

## วิธีใช้งานผ่าน API

### Endpoint

```
POST /api/import/observe
```

### ตัวอย่าง Request

#### 1. นำเข้าข้อมูลวันเดียว

```json
{
  "utokID": "8",
  "startDate": "11/11/2568"
}
```

#### 2. นำเข้าข้อมูลช่วงวันที่

```json
{
  "utokID": "8",
  "startDate": "09/11/2568",
  "endDate": "13/11/2568"
}
```

### Response

```json
{
  "success": true,
  "message": "Data imported successfully",
  "imported": 15,
  "updated": 0,
  "failed": 0,
  "total": 15,
  "dateRange": "09/11/2568 - 13/11/2568",
  "failedDates": []
}
```

### กรณีมีข้อผิดพลาด

```json
{
  "success": true,
  "message": "Data imported with some errors",
  "imported": 10,
  "updated": 2,
  "failed": 3,
  "total": 12,
  "dateRange": "09/11/2568 - 13/11/2568",
  "failedDates": [
    {
      "date": "10/11/2568",
      "error": "No data available"
    },
    {
      "date": "11/11/2568",
      "error": "API timeout"
    },
    {
      "date": "12/11/2568",
      "error": "Database error: Duplicate entry"
    }
  ]
}
```

## รูปแบบวันที่

- **รูปแบบที่รองรับ:** `DD/MM/YYYY` (พ.ศ.)
- **ตัวอย่าง:** `11/11/2568` คือ วันที่ 11 พฤศจิกายน 2568

ระบบจะแปลงเป็น ค.ศ. โดยอัตโนมัติ (`11/11/2568` → `2025-11-11`)

## ข้อจำกัดและข้อควรระวัง

1. **Rate Limiting:** ระบบรอ 1 วินาทีระหว่างการเรียก API แต่ละวัน
2. **Timeout:** แต่ละ request มี timeout 30 วินาที
3. **ข้อมูลที่ได้:** ระบบจะดึงเฉพาะสถานี X.274, X.119A, และ X.119
4. **การจัดการข้อผิดพลาด:** 
   - หากวันใดวันหนึ่งล้มเหลว ระบบจะดำเนินการต่อกับวันถัดไป
   - วันที่ล้มเหลวจะถูกบันทึกใน `failedDates` array

## ตัวอย่างการใช้งานด้วย cURL

### วันเดียว

```bash
curl -X POST http://localhost:5000/api/import/observe \
  -H "Content-Type: application/json" \
  -d '{
    "utokID": "8",
    "startDate": "11/11/2568"
  }'
```

### ช่วงวันที่

```bash
curl -X POST http://localhost:5000/api/import/observe \
  -H "Content-Type: application/json" \
  -d '{
    "utokID": "8",
    "startDate": "09/11/2568",
    "endDate": "13/11/2568"
  }'
```

## ตัวอย่างการใช้งานด้วย PowerShell

```powershell
$body = @{
    utokID = "8"
    startDate = "09/11/2568"
    endDate = "13/11/2568"
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:5000/api/import/observe" `
    -Method POST `
    -ContentType "application/json" `
    -Body $body
```

## การตรวจสอบผลลัพธ์

หลังจากนำเข้าข้อมูลสำเร็จ ตรวจสอบได้โดย:

1. **ผ่าน Frontend:** รีเฟรชหน้า Observe Data
2. **ผ่าน API:**
   ```bash
   curl http://localhost:5000/api/waterlevel/chart?start_date=2025-11-09&end_date=2025-11-13
   ```
3. **ผ่าน Database:**
   ```sql
   SELECT date_time, x_274, x_119a, x_119, data_type 
   FROM waterlevel_data 
   WHERE data_type = 'observe' 
   AND date_time BETWEEN '2025-11-09' AND '2025-11-13'
   ORDER BY date_time;
   ```

## Best Practices

1. **ดึงข้อมูลครั้งละไม่เกิน 30 วัน** เพื่อหลีกเลี่ยง timeout
2. **ตรวจสอบ failedDates** หลังการนำเข้า หากมีวันที่ล้มเหลวให้ลองนำเข้าใหม่
3. **รันในช่วงที่ไม่ใช้งานหนัก** สำหรับการดึงข้อมูลย้อนหลังจำนวนมาก
4. **ตรวจสอบ logs** ที่ backend เพื่อดูรายละเอียดการทำงาน

## Troubleshooting

### ปัญหา: API timeout
- **สาเหตุ:** เครือข่ายช้า หรือ API ของ กรมชลประทาน ตอบสนองช้า
- **แก้ไข:** ลองนำเข้าวันที่นั้นอีกครั้ง หรือลดจำนวนวันที่ดึงในแต่ละครั้ง

### ปัญหา: No data available
- **สาเหตุ:** API ไม่มีข้อมูลสำหรับวันที่นั้น
- **แก้ไข:** ตรวจสอบว่าวันที่ถูกต้อง และเป็นวันที่มีข้อมูลจริง

### ปัญหา: Database error
- **สาเหตุ:** ข้อมูลซ้ำ หรือ schema ไม่ถูกต้อง
- **แก้ไข:** ตรวจสอบ logs และ database schema

## ข้อมูลเพิ่มเติม

- **API ต้นทาง:** กรมชลประทาน (RID)
- **เอกสารอื่นๆ:**
  - [CSV_IMPORT_GUIDE.md](CSV_IMPORT_GUIDE.md) - คู่มือการนำเข้าข้อมูล Forecast จาก CSV
  - [API_STATUS.md](API_STATUS.md) - สถานะและข้อมูล API
  - [QUICKSTART.md](QUICKSTART.md) - คู่มือเริ่มต้นใช้งาน
